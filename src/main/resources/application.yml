########## Spring Properties ##########
server:
  servlet:
    context-path: /pharmacy-app
  port: 5444
spring:
  application:
    name: pharmacy-app
  datasource:    
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

########## Application Properties ##########
pharmacyapp:
  client-id: irm-client
  client-secret: irm-secret
